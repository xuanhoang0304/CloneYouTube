import UploadContainer from './UploadContainer';

const Upload = () => {
    return (
        <section className=" w-full pt-[86px] px-3">
            <UploadContainer></UploadContainer>
        </section>
    );
};

export default Upload;
