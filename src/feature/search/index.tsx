import SearchVideoList from './components/SearchVideoList';

const SearchPage = () => {
    return (
        <section className="pt-[66px] lg:pl-[269px] w-[calc(100%-24px)] mx-auto lg:w-full lg:pr-[30px] pb-6">
            <SearchVideoList></SearchVideoList>
        </section>
    );
};

export default SearchPage;
