"use client";

const SubscriptionsList = () => {
    // const { data: subscriptions } = useApi({
    //     url: `https://www.googleapis.com/youtube/v3/subscriptions?part=snippet&mine=true&key=${process.env.NEXT_PUBLIC_API_KEY}`,
    // });
    return (
        <>
            {/* <ul>
                {sortChannelsByStatus(channels)
                    .slice(0, itemCount)
                    .map((item: ChanelType) => {
                        return (
                            <SubscriptionsItem
                                key={item.id}
                                item={item}
                            ></SubscriptionsItem>
                        );
                    })}
            </ul>
            <ShowMoreChannel
                onShowMore={handleShowMore}
                itemCount={itemCount}
                channelLength={channelLength}
            ></ShowMoreChannel> */}
        </>
    );
};

export default SubscriptionsList;
